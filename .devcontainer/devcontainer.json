{
  "name": "aws-bootcamp",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "set -e; sudo apt-get update && sudo apt-get install -y unzip curl && curl -sS https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip -o /tmp/awscliv2.zip && cd /tmp && unzip -q awscliv2.zip && sudo ./aws/install --update && aws --version"
}
